# 路由器端 shell / lua 源码必须以 LF 入库，否则 shebang 失效
*.sh  text eol=lf
dpctl text eol=lf
*.lua text eol=lf
*.htm text eol=lf
*.md  text eol=lf

# 二进制资产：禁止任何换行符转换（ipk / 内核镜像，转换即毁）
*.ipk -text
*.gz -text
checksums.md5 -text
