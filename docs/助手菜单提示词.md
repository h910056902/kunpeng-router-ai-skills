# 助手菜单提示词（一段话启动器）

> **2026-09-19 改版**：菜单协议本体已归一到 [`AGENTS.md`](../AGENTS.md) §8「菜单助手协议」。
> 本文件只保留「一段话启动器」+ 维护须知，**不再复制菜单原文** —— 以前同一段菜单文本在仓库里存了三份，
> 改一处漏两处（真翻过车）。

## 一段话启动器（复制即用）

把下面这一段发给 AI —— **仓库地址已写在段内**，不必另外告知：

```text
你现在是「鲲鹏 C2000 U 路由器 · 安装助手」，运行在仓库 kunpeng-router-ai-skills 之上（https://github.com/h910056902/kunpeng-router-ai-skills；本地没有就先 clone 它）：先静默读该仓库的 AGENTS.md 与 tasks/index.json（读的过程中一个字都不要输出），然后严格按 AGENTS.md §8「菜单助手协议」工作 —— 第一条回复只输出协议规定的图标行和菜单代码块，别的什么都别写，然后停下等我输入编号；我输入编号后按协议执行对应功能（每个功能四关：前置 / 执行 / 验证 / 收尾），跑完回到菜单等我下一步；协议里的 10 条硬约束全程有效，其中 4) 是交互式脚本，那一步只能我自己按菜单，你必须停下等我。
```

它读完仓库后会：静默加载 → **第一条回复只弹图标行 + 菜单代码块** → 停下等你输编号 → 按四关执行 →
回到菜单。菜单原文、输入解析、四关、4) 的交互式细则、10 条硬约束，全部在 `AGENTS.md` §8，不用你贴。

## 生效条件（缺一不可）

1. AI 能读到仓库（`AGENTS.md` + `tasks/index.json` 可达 —— 地址写在启动器段内）；
2. AI 能连到设备（`ROUTER_HOST` / `ROUTER_USER` / `ROUTER_PW` 在环境变量里）；
3. 选 4) 时那一步必须有**真终端**（脚本是交互式的，见 §8.6）。

## 维护须知

- **唯一真源**：菜单文本只存在于 `AGENTS.md` §8.2。改菜单 → 改 §8.2 → 重放 `_readme_template.md` → 重跑自检。
- `README.md` 里那份菜单是**展示预览**。`_selfcheck.py` 会把 `AGENTS.md` §8.0 的启动器与 §8.2 的菜单块
  跟 README 做**逐字同源校验**，飘了直接 FAIL —— 这就是「只留一处真源」的保险。
- 图标资产在 `assets/menu/`（32px PNG）。新增一项要补一枚图标、更新 §8.1 的图标行、并更新自检的资产清单。
- 三个「为什么」（首弹为什么只有菜单 / 菜单为什么必须放代码块 / 4) 为什么要把第②关拆两半）都写在 §8 各段末尾，
  别挪走 —— 它们是踩过的坑的记录。

## 为什么 4) 必须单列（不能照 1/2/3 的写法）

1. **它是交互式的，AI 替不了。** 启动 = 清屏 + 10 秒免责声明倒计时 + 读 stdin 输入 `y`；管道、`exec_command`、
   CI 下都会 `die "input cancelled"` 或挂住 → 第②关必须**拆两半**并写明「停下等使用者」。
2. **它菜单里有能毁设备的选项。** playbook 通篇是「怎么装」，AI 不会自己拦住「卸载 Docker」
   （`rm -f /etc/config/dockerd` = 1Panel 环境 + 容器数据一起报废）→ 红线必须写进**协议硬约束**，
   不能只躺在 playbook 里（playbook 给愿意读文档的 AI，硬约束给「直接开干」的 AI）。
3. **它不产生任何备份。** `backup_file()` 是空实现（`return 0`），`BACKUP_DIR="/root/nradio-plugin-fix"`
   是死路径（真机 `ls -ld` → No such file）。所以「跑前自己备份」是硬前置，不是可选项。
4. **版本门禁有坑。** 判据读 `ubus call system board` 的 `release.revision` = `2.3.0.n0.c1`，
   而不是 `/etc/openwrt_release` 的 `DISTRIB_RELEASE='21.02-SNAPSHOT'`；只看后者会误判「会被脚本拒绝」。
