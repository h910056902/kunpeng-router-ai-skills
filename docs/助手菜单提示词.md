# 助手菜单提示词（一段话启动器）

> **2026-09-19 改版**：菜单协议本体已归一到 [`AGENTS.md`](../AGENTS.md) §8「菜单助手协议」。
> 本文件只保留「一段话启动器」+ 维护须知，**不再复制菜单原文** —— 以前同一段菜单文本在仓库里存了三份，
> 改一处漏两处（真翻过车）。
> **2026-09-19 扩容**：菜单从 4 项扩到 9 项（5)~9) 是第三方脚本的五个功能分类），
> 分类**不是**「跑一遍 tasks/05 就行」—— 每个分类有自己的红线与判据，见 `tasks/06`~`tasks/10`。

## 一段话启动器（复制即用）

把下面这一段发给 AI —— 仓库地址、SSH 登录信息（在 `AGENTS.md` §1 设备档案里）都在仓库内，提示词只负责「你是谁、去哪读、读完弹菜单」：

```text
你现在是「鲲鹏 C2000 U 路由器 · 安装助手」，先静默读完仓库 kunpeng-router-ai-skills（https://github.com/h910056902/kunpeng-router-ai-skills；GitHub 访问不了就改用 CDN 直读 https://cdn.jsdelivr.net/gh/h910056902/kunpeng-router-ai-skills@main/AGENTS.md 和 https://cdn.jsdelivr.net/gh/h910056902/kunpeng-router-ai-skills@main/tasks/index.json）里的 AGENTS.md 与 tasks/index.json，读完前不要输出任何内容。
之后一切都按仓库 AGENTS.md 的指引来：先按 AGENTS.md §8「菜单助手协议」弹出菜单；怎么 SSH 登录路由器（§1 设备档案）、怎么连接与传文件（§6）、每个功能怎么做、哪些事不能做，仓库里都写清楚了，照做即可；做完回到菜单等我下一步。
```

它读完仓库后会：静默加载 → **第一条回复按 §8.1 弹「图标行 + 一句用法提示 + 菜单代码块」**
（菜单块尾带 maye 助手致谢与自研版权声明）→ 停下等你输编号 → 按四关执行 → 回到菜单。
菜单原文、输入解析、四关、4)~9) 的交互式细则、10 条硬约束，全部在 `AGENTS.md` §8，不用你贴。

## 九个功能分别是什么

| 菜单 | 是什么 | playbook |
|---|---|---|
| 1~3 | 我们自己实现的三件事：OpenClash 安装 / ocspeed 安装 / Docker + 1Panel 安装 | `tasks/01`、`tasks/02`、`tasks/03` |
| **4** | 第三方 NROS 插件安装器（maye 助手）**总入口** —— 走它自己的主菜单，你自己选分类 | `tasks/05-nros-plugin-installer.md` |
| **5** | 分类一 · 常用插件安装（swap / 哈基米 / WebSSH / AdGuardHome / OpenList / MosDNS / DDNS-GO / Docker / MT5700 / Open-Box） | `tasks/06-nros-plugins-common.md` |
| **6** | 分类二 · VPN / 组网 / 路由向导（ZeroTier / EasyTier / OpenVPN + 三个路由向导） | `tasks/07-nros-network-route.md` |
| **7** | 分类三 · 游戏加速器（奇游 / 雷神） | `tasks/08-nros-game-accel.md` |
| **8** | 分类四 · 应用商店与页面美化（美化 / 还原 / OpenWrt 原版 LuCI 8080） | `tasks/09-nros-appcenter-polish.md` |
| **9** | 分类五 · 设备维护与检测（统一体检 / 封版工具箱 / 硬速加速 / 首页温度…） | `tasks/10-nros-maintenance.md` |

**4) 与 5)~9) 是同一个交互式脚本**：`4)` 是总入口（主菜单由你选分类），`5)~9)` 是「已指定分类」的直达入口。
两者 AI 的职责完全相同（只有前置 / 下载 / 校验 / 备份 / 基线），区别只在于**该分类专属的红线与判据**。

## 生效条件（缺一不可）

1. AI 能读到仓库（`AGENTS.md` + `tasks/index.json` 可达 —— 地址写在启动器段内）；
2. AI 能连到设备（SSH 地址与密码在 `AGENTS.md` §1 设备档案里，AI 读完仓库即知）；
3. 选 4)~9) 时那一步必须有**真终端**（脚本是交互式的，见 §8.6）。

## 维护须知

- **唯一真源**：菜单文本只存在于 `AGENTS.md` §8.2。改菜单 → 改 §8.2 → 重放 `_readme_template.md` → 重跑自检。
- `README.md` 里那份菜单是**展示预览**。`_selfcheck.py` 会把 `AGENTS.md` §8.0 的启动器与 §8.2 的菜单块
  跟 README 做**逐字同源校验**，飘了直接 FAIL —— 这就是「只留一处真源」的保险。
- 图标资产在 `assets/menu/`（10 枚 32px PNG：1~4 对应 openclash / ocspeed / 1panel / docker + maye，
  5~9 对应 plugins / network / game / store / maint）。
  **新增一项要补一枚图标、更新 §8.1 的图标行、并更新自检的资产清单**（三处必须同改）。
- 新增/修改一个**分类**时，除了改 §8.2 菜单，还要：新增 `tasks/NN-*.md` 分册 → 加 `tasks/index.json` 条目
  → 更新 §8.5 表与 §8.6.1 差异速查 → 更新 §8.7 第 7 条的禁选项 → 改任务计数（4 处文档）→ 重跑自检。
- 四个「为什么」（首弹为什么只有菜单 / 菜单为什么必须放代码块 / 4)~9) 为什么要把第②关拆两半 /
  为什么 9) 的编号是 `0-8 / 11-12` 跳号）都写在 §8 各段末尾，别挪走 —— 它们是踩过的坑的记录。

## 为什么 4)~9) 必须单列（不能照 1/2/3 的写法）

1. **它是交互式的，AI 不该替它做选择。** 启动 = 清屏 + 10 秒免责声明倒计时 + 读 stdin 输入 `y`。
   实测三种喂法：`< /dev/null` → `die "input cancelled"`；`exec_command` 不喂不关 → **永久挂住**；
   管道喂够 `y`+编号 → 其实能跑通（rc=0）。**技术上能自动化，但绝不许** —— 那等于替使用者选菜单项，
   而菜单里有「卸载 Docker」「还原应用商店」。→ 第②关必须**拆两半**并写明「停下等使用者」。
2. **它菜单里有能毁设备的选项。** playbook 通篇是「怎么装」，AI 不会自己拦住「卸载 Docker」
   （`rm -f /etc/config/dockerd` = 1Panel 环境 + 容器数据一起报废）→ 红线必须写进**协议硬约束**，
   不能只躺在 playbook 里（playbook 给愿意读文档的 AI，硬约束给「直接开干」的 AI）。
   现在红线按分类拆成 7 类（§8.7 第 7 条），例如「哈基米＝装 OpenClash」「还原应用商店＝抹掉补丁 marker」
   「`5 › 11` 硬件加速＝`fw3 reload`」。
3. **它不产生任何备份。** `backup_file()` 是空实现（`return 0`），`BACKUP_DIR="/root/nradio-plugin-fix"`
   是死路径（真机 `ls -ld` → No such file）。所以「跑前自己备份」是硬前置，不是可选项。
4. **版本门禁有坑。** 判据读 `ubus call system board` 的 `release.revision` = `2.3.0.n0.c1`，
   而不是 `/etc/openwrt_release` 的 `DISTRIB_RELEASE='21.02-SNAPSHOT'`；只看后者会误判「会被脚本拒绝」。
5. **每个分类要挡的东西不一样。** 6) 挡的是「`ip rule` 一改就断网」，7) 挡的是「明文 HTTP 下载的 root 脚本」，
   8) 挡的是「商店补丁被抹」，9) 挡的是「`fw3 reload` 重建 nat 链」。
   把这些混在一份 tasks/05 里写，AI 读不过来 —— 所以拆成 `tasks/06`~`tasks/10` 五份分册。
